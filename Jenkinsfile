pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                sh "ls"
                // sh "cat Dockerfile"
                echo 'Hello World'
            }
        }
      
    }
}
